# 📦 Микросервисная архитектура: контроллеры и функционал

Ниже приведено описание контроллеров всех сервисов платформы, их назначения и реализуемого функционала.

---

## 🏗 Модуль Core

Содержит основные бизнес-сервисы платформы.

---

### interaction-api

**Назначение:**  
Общие DTO и контракты для Core-сервисов.

**Содержит:**
- Общие модели данных (DTO)
- Исключения и обработку исключений
- Feign-клиенты для взаимодействия микросервисов

---

## 💬 comment-service

Сервис работы с комментариями.

---

### CommentsAdminController

**Назначение:**  
Управление комментариями от лица администратора.

**Endpoints:**
- **GET** `/admin/comments`  
  Получение всех комментариев (постранично)

- **DELETE** `/admin/comments/{commentId}`  
  Удаление комментария по ID

---

### CommentsPrivateController

**Назначение:**  
Работа с комментариями для авторизованных пользователей.

**Endpoints:**
- **POST** `/events/{eventId}/comments`  
  Создание комментария

- **PATCH** `/events/{eventId}/comments`  
  Редактирование комментария

- **DELETE** `/events/{eventId}/comments`  
  Удаление комментария пользователем

---

### CommentsPublicController

**Назначение:**  
Публичный просмотр комментариев.

**Endpoints:**
- **GET** `/events/{eventId}/comments`  
  Получение комментариев к событию (постранично)

---

## 🎯 event-service

Сервис управления событиями, категориями и подборками.

---

### CategoryAdminController

**Назначение:**  
Управление категориями (администратор).

**Endpoints:**
- **POST** `/admin/categories`  
  Создание категории

- **PATCH** `/admin/categories/{catId}`  
  Обновление категории

- **DELETE** `/admin/categories/{catId}`  
  Удаление категории

---

### CategoryController

**Назначение:**  
Публичный доступ к категориям.

**Endpoints:**
- **GET** `/categories`  
  Получение списка категорий

- **GET** `/categories/{catId}`  
  Получение категории по ID

---

### CompilationAdminController

**Назначение:**  
Управление подборками событий (администратор).

**Endpoints:**
- **POST** `/admin/compilations`  
  Создание подборки

- **DELETE** `/admin/compilations/{compId}`  
  Удаление подборки

- **PATCH** `/admin/compilations/{compId}`  
  Обновление подборки

---

### CompilationController

**Назначение:**  
Публичный доступ к подборкам.

**Endpoints:**
- **GET** `/compilations`  
  Получение списка подборок

- **GET** `/compilations/{compId}`  
  Получение подборки по ID

---

### EventAdminController

**Назначение:**  
Управление событиями (администратор).

**Endpoints:**
- **GET** `/admin/events`  
  Поиск событий по фильтрам

- **PATCH** `/admin/events/{eventId}`  
  Обновление события

---

### EventController

**Назначение:**  
Публичный доступ к событиям.

**Endpoints:**
- **GET** `/events`  
  Получение списка событий по критериям

- **GET** `/events/{id}`  
  Получение события по ID

---

### EventPrivateController

**Назначение:**  
Работа с событиями от лица пользователя.

**Endpoints:**
- **GET** `/users/{userId}/events`  
  Получение списка событий пользователя

- **POST** `/users/{userId}/events`  
  Создание события

- **GET** `/users/{userId}/events/{eventId}`  
  Получение события пользователя

- **PATCH** `/users/{userId}/events/{eventId}`  
  Редактирование события

---

## 📥 request-service

Сервис обработки заявок на участие в событиях.

---

### EventRequestController

**Назначение:**  
Управление заявками на участие в событии (организатор).

**Endpoints:**
- **GET** `/users/{userId}/events/{eventId}/requests`  
  Получение заявок на участие

- **PATCH** `/users/{userId}/events/{eventId}/requests`  
  Обновление статуса заявок

---

### RequestController

**Назначение:**  
Управление заявками пользователя.

**Endpoints:**
- **GET** `/users/{userId}/requests`  
  Получение заявок пользователя

- **POST** `/users/{userId}/requests`  
  Создание заявки

- **PATCH** `/users/{userId}/requests/{requestId}/cancel`  
  Отмена заявки

---

## 👤 user-service

Сервис управления пользователями.

---

### UserController

**Назначение:**  
CRUD-операции с пользователями (администратор).

**Endpoints:**
- **POST** `/admin/users`  
  Создание пользователя

- **GET** `/admin/users`  
  Получение списка пользователей

- **GET** `/admin/users/{userId}`  
  Получение пользователя по ID

- **DELETE** `/admin/users/{userId}`  
  Удаление пользователя

---

## 📊 Модуль Stats

Сервисы сбора и предоставления статистики.

---

### stats-server

### StatsClient (Feign-клиент)

**Назначение:**  
Взаимодействие с сервисом статистики.

**Endpoints:**
- **POST** `/hit`  
  Отправка данных о посещении

- **GET** `/stats`  
  Получение статистики по параметрам

---

## ⚙️ Модуль Infra

Инфраструктурные сервисы для конфигурации, маршрутизации и обнаружения.

---

### config-server

**Технология:** `spring-cloud-config-server`

**Назначение:**  
Централизованное хранение конфигураций микросервисов.

**Особенности:**
- Хранение конфигурационных файлов всех сервисов
- Регистрация в Eureka (discovery-server)
- Предоставление конфигураций клиентским сервисам

---

### discovery-server

**Технология:** `spring-cloud-starter-netflix-eureka-server`

**Назначение:**  
Сервис регистрации и обнаружения микросервисов.

**Особенности:**
- Реестр всех микросервисов
- Балансировка нагрузки
- Поиск сервисов по имени

---

### gateway-server

**Технология:** `spring-cloud-starter-gateway`

**Назначение:**  
API Gateway — единая точка входа в систему.

**Особенности:**
- Маршрутизация запросов к микросервисам
- Применение фильтров (аутентификация, логирование, защита от DDoS)
